import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const globalPageSlice = createSlice({
  name: "globalPage",
  initialState,
  reducers: {},
});

export const {} = globalPageSlice.actions;
export default globalPageSlice.reducer;
